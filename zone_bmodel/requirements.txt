# ------------------------------------------------------------
# Core scientific stack
# ------------------------------------------------------------
numpy==2.1.2
opencv-python==4.10.0.84
pandas==2.2.3
matplotlib==3.9.2
seaborn==0.13.2
scipy==1.14.1

# ------------------------------------------------------------
# PyTorch + CUDA 12.4 wheels (works on Python >=3.9, Win/Linux)
# Keep PyPI as the primary index and add the torch repo as extra
# ------------------------------------------------------------
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.5.1+cu124
torchvision==0.20.1+cu124

# If you need CPU-only builds instead, comment the block above and
# uncomment the following block:
# --extra-index-url https://download.pytorch.org/whl/cpu
# torch==2.5.1
# torchvision==0.20.1

# ------------------------------------------------------------
# Vision / analytics utilities
# ------------------------------------------------------------
ultralytics==8.0.186
streamlit==1.39.0
