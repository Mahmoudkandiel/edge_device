# ------------------------------------------------------------
# CuTrack Requirements for SOPHON Edge Deployment
# ------------------------------------------------------------
# This file contains dependencies optimized for Sophon TPU devices
# Note: Sophon SDK (sophon.sail) must be installed separately
# from the Sophon SDK package, not via pip
# ------------------------------------------------------------

# ------------------------------------------------------------
# Core scientific stack (CPU-only versions for ARM64)
# ------------------------------------------------------------
numpy==2.1.2
opencv-python==4.10.0.84
pandas==2.2.3
matplotlib==3.9.2
seaborn==0.13.2
scipy==1.14.1
Pillow==10.4.0

# ------------------------------------------------------------
# Note: PyTorch/Ultralytics are NOT required for Sophon deployment
# The code will automatically use Sophon BModel if available
# However, if you want fallback support, you can install:
# ------------------------------------------------------------
# PyTorch CPU-only (optional, for fallback):
# --extra-index-url https://download.pytorch.org/whl/cpu
# torch==2.5.1
# torchvision==0.20.1
# ultralytics==8.0.186

# ------------------------------------------------------------
# Web interface (optional)
# ------------------------------------------------------------
streamlit==1.39.0

# ------------------------------------------------------------
# SOPHON SDK Installation Notes:
# ------------------------------------------------------------
# 1. Download Sophon SDK from: https://developer.sophgo.com/
# 2. Extract and install according to Sophon documentation
# 3. Set environment variables:
#    export LD_LIBRARY_PATH=/opt/sophon/libsophon/lib:$LD_LIBRARY_PATH
#    export PYTHONPATH=/opt/sophon/sophon-sail/python3/lib:$PYTHONPATH
# 4. Verify installation:
#    python3 -c "import sophon.sail as sail; print('SOPHON SDK OK')"
# ------------------------------------------------------------

